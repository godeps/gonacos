/gonacos
/bin/
/dist/
/coverage.out
/tmp/
/web/dist/
/.omc/
/.saker-workspace-active
/.gonacos/
**/.gonacos/
/.last-run.json
# Stale binaries produced by `go run ./examples/<name>` at repo root
/auth
/config
/namespace
/naming

# React SPA build artifacts. node_modules is always ignored; dist/ is
# committed so `go build` works out of the box without running `make web`.
# Run `make web` after changing the React UI to regenerate dist/.
/pkg/web/console-ui/node_modules/

